# =============================================================================
# SECURITY — never commit credentials
# =============================================================================
gcp-key.json
*.json.key
*credentials*.json
.env
secrets/

# =============================================================================
# RESULTS & CACHES — all live in GCS, not in the repo
# =============================================================================

# Stage caches (model-level resumption state)
stage_cache/
stage_cache_ablation/
stage_cache_prod*/

# Data caches (downloaded parquet files)
data_cache/
data_cache_production/

# Local result outputs
results/
baseline_results.*
ablation_results.*

# Production result files — live in GCS, exported locally but not versioned
production_results.*
lead_time_results.*
hmm_robustness_label_agreement.*
hmm_robustness_xgb_comparison.*

# HMM robustness checkpoint files (large intermediates)
robustness_train_checkpoint.*
robustness_test_checkpoint.*

# =============================================================================
# PAPER FIGURES — generated by 08_generate_paper_figures.py
# Binary PNG files, reproducible from scripts, no place in git history
# =============================================================================
paper_figures/
images/

# =============================================================================
# DATA FILES — binary formats, live in GCS
# =============================================================================
*.parquet
*.csv

# =============================================================================
# DATA ARTEFACTS — result JSONs from training runs
# =============================================================================
ig_lstm_binary.json
ig_lstm_multiclass.json

# =============================================================================
# LOGS — operational, not scientific artefacts
# =============================================================================
logs/
*.log
nohup.out

# =============================================================================
# JUPYTER CHECKPOINTS
# =============================================================================
.ipynb_checkpoints/
**/.ipynb_checkpoints/
scripts/.ipynb_checkpoints/

# =============================================================================
# PYTHON
# =============================================================================
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg
.Python

# =============================================================================
# VIRTUAL ENVIRONMENTS
# =============================================================================
venv/
env/
.venv/
.env/

# =============================================================================
# TEMPORARY FILES
# =============================================================================
/tmp/
*.tmp
*.temp
*.parquet.tmp

# =============================================================================
# OS FILES
# =============================================================================
.DS_Store
Thumbs.db
desktop.ini

# =============================================================================
# MODEL WEIGHTS — large files, live in GCS
# =============================================================================
*.pt
*.pkl
*.h5
*.onnx

# =============================================================================
# WRITE PROBES
# =============================================================================
.write_probe


 PERSONAL OPS SCRIPTS — one-off/manual utilities, not part of the pipeline
# =============================================================================
scripts/sync_models_to_hf.py
scripts/sync_results_to_hf.py
scripts/check_gcs_models.py
scripts/check_rf_size.py
.hf_sync_tmp/